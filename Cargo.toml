[package]
name = "using_js_bytecode"
version = "0.1.0"
edition = "2021"

[workspace]
members = ["runtime", "server", "experimentation"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[dependencies]
runtime = { path = "./runtime" }
